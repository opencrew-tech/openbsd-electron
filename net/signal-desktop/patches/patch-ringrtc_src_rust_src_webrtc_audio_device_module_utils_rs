Index: ringrtc/src/rust/src/webrtc/audio_device_module_utils.rs
--- ringrtc/src/rust/src/webrtc/audio_device_module_utils.rs.orig
+++ ringrtc/src/rust/src/webrtc/audio_device_module_utils.rs
@@ -14,7 +14,7 @@ use std::{
 
 use anyhow::anyhow;
 use cubeb::{DeviceCollection, DeviceState};
-#[cfg(target_os = "linux")]
+#[cfg(any(target_os = "openbsd", target_os = "linux"))]
 use cubeb_core::DeviceType;
 use cubeb_core::{DeviceId, DevicePref};
 
@@ -28,7 +28,7 @@ pub struct MinimalDeviceInfo {
     pub devid: DeviceId,
     pub device_id: Option<String>,
     pub friendly_name: String,
-    #[cfg(target_os = "linux")]
+    #[cfg(any(target_os = "openbsd", target_os = "linux"))]
     device_type: DeviceType,
     preferred: DevicePref,
     state: DeviceState,
@@ -46,7 +46,7 @@ pub struct DeviceCollectionWrapper {
     device_collection: Vec<MinimalDeviceInfo>,
 }
 
-#[cfg(target_os = "linux")]
+#[cfg(any(target_os = "openbsd", target_os = "linux"))]
 fn device_is_monitor(device: &MinimalDeviceInfo) -> bool {
     device.device_type == DeviceType::INPUT
         && device
@@ -64,7 +64,7 @@ impl DeviceCollectionWrapper {
                     devid: device.devid(),
                     device_id: device.device_id().as_ref().map(|s| s.to_string()),
                     friendly_name: friendly.to_string(),
-                    #[cfg(target_os = "linux")]
+                    #[cfg(any(target_os = "openbsd", target_os = "linux"))]
                     device_type: device.device_type(),
                     preferred: device.preferred(),
                     state: device.state(),
@@ -89,7 +89,7 @@ impl DeviceCollectionWrapper {
     }
 
     // For linux only, a method that will ignore "monitor" devices.
-    #[cfg(target_os = "linux")]
+    #[cfg(any(target_os = "openbsd", target_os = "linux"))]
     pub fn iter_non_monitor(
         &self,
     ) -> std::iter::Filter<std::slice::Iter<'_, MinimalDeviceInfo>, fn(&&MinimalDeviceInfo) -> bool>
@@ -131,7 +131,7 @@ impl DeviceCollectionWrapper {
             {
                 self.iter().nth(idx - 1)
             }
-            #[cfg(target_os = "linux")]
+            #[cfg(any(target_os = "openbsd", target_os = "linux"))]
             {
                 // filter out "monitor" devices.
                 self.iter_non_monitor().nth(idx - 1)
@@ -159,12 +159,12 @@ impl DeviceCollectionWrapper {
     pub fn count(&self) -> usize {
         #[cfg(target_os = "macos")]
         let count = self.iter().count();
-        #[cfg(target_os = "linux")]
+        #[cfg(any(target_os = "openbsd", target_os = "linux"))]
         let count = self.iter_non_monitor().count();
         if count == 0 {
             #[cfg(target_os = "macos")]
             return 0;
-            #[cfg(target_os = "linux")]
+            #[cfg(any(target_os = "openbsd", target_os = "linux"))]
             return
                 // edge case: if there are only monitor devices, and one is the default,
                 // allow it.
@@ -487,7 +487,7 @@ pub fn do_cubeb_redactions(text: Cow<str>) -> Cow<str>
 
 #[cfg(test)]
 mod audio_device_module_tests {
-    #[cfg(target_os = "linux")]
+    #[cfg(any(target_os = "openbsd", target_os = "linux"))]
     use cubeb_core::DeviceType;
 
     use super::*;
@@ -825,7 +825,7 @@ mod audio_device_module_tests {
                     devid: std::ptr::null(),
                     device_id: Some("devid1".to_string()),
                     friendly_name: "Device 1".to_string(),
-                    #[cfg(target_os = "linux")]
+                    #[cfg(any(target_os = "openbsd", target_os = "linux"))]
                     device_type: DeviceType::INPUT,
                     preferred: DevicePref::all(),
                     state: DeviceState::Enabled,
@@ -834,7 +834,7 @@ mod audio_device_module_tests {
                     devid: std::ptr::null(),
                     device_id: Some("devid2".to_string()),
                     friendly_name: "Device 2".to_string(),
-                    #[cfg(target_os = "linux")]
+                    #[cfg(any(target_os = "openbsd", target_os = "linux"))]
                     device_type: DeviceType::INPUT,
                     preferred: DevicePref::empty(),
                     state: DeviceState::Enabled,
@@ -901,7 +901,7 @@ mod audio_device_module_tests {
                     devid: std::ptr::null(),
                     device_id: Some("devid1".to_string()),
                     friendly_name: "Device 1".to_string(),
-                    #[cfg(target_os = "linux")]
+                    #[cfg(any(target_os = "openbsd", target_os = "linux"))]
                     device_type: DeviceType::INPUT,
                     preferred: DevicePref::empty(),
                     state: DeviceState::Enabled,
@@ -910,7 +910,7 @@ mod audio_device_module_tests {
                     devid: std::ptr::null(),
                     device_id: Some("devid2".to_string()),
                     friendly_name: "Device 2".to_string(),
-                    #[cfg(target_os = "linux")]
+                    #[cfg(any(target_os = "openbsd", target_os = "linux"))]
                     device_type: DeviceType::INPUT,
                     preferred: DevicePref::empty(),
                     state: DeviceState::Enabled,
