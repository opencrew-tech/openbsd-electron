Use hardcoded path for unittests

Index: src/gn/command_format_unittest.cc
--- src/gn/command_format_unittest.cc.orig
+++ src/gn/command_format_unittest.cc
@@ -31,7 +31,7 @@ using FormatTest = TestWithScheduler;
     std::string input;                                                         \
     std::string out;                                                           \
     std::string expected;                                                      \
-    base::FilePath src_dir = GetFormatTestDataDir();                           \
+    base::FilePath src_dir = base::FilePath("${WRKSRC}");                      \
     ASSERT_TRUE(base::SetCurrentDirectory(src_dir))                            \
         << "Failed to set current directory to: " << FilePathToUTF8(src_dir);  \
     base::FilePath current_dir;                                                \
