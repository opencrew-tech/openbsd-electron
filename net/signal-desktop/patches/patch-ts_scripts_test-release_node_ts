Index: ts/scripts/test-release.node.ts
--- ts/scripts/test-release.node.ts.orig
+++ ts/scripts/test-release.node.ts
@@ -14,7 +14,7 @@ import { gracefulRmRecursive } from '../util/gracefulF
 import { consoleLogger } from '../util/consoleLogger.std.js';
 
 const ENVIRONMENT = 'production';
-const RELEASE_DIR = join(__dirname, '..', '..', 'release');
+const RELEASE_DIR = join(__dirname, '..', '..', 'dist');
 
 let archive: string;
 let exe: string;
@@ -36,7 +36,7 @@ if (process.platform === 'darwin') {
 } else if (process.platform === 'win32') {
   archive = join('win-unpacked', 'resources', 'app.asar');
   exe = join('win-unpacked', `${productName}.exe`);
-} else if (process.platform === 'linux') {
+} else if (process.platform === 'linux' || process.platform === 'openbsd') {
   archive = join('linux-unpacked', 'resources', 'app.asar');
   exe = join('linux-unpacked', name);
 } else {
