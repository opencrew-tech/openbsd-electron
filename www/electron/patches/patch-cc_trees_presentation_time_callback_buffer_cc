Index: cc/trees/presentation_time_callback_buffer.cc
--- cc/trees/presentation_time_callback_buffer.cc.orig
+++ cc/trees/presentation_time_callback_buffer.cc
@@ -131,7 +131,9 @@ PresentationTimeCallbackBuffer::GetOrMakeRegistration(
   if (frame_token_infos_.empty() ||
       viz::FrameTokenGT(frame_token, frame_token_infos_.back().token)) {
     frame_token_infos_.emplace_back(frame_token);
-    DCHECK_LE(frame_token_infos_.size(), kMaxBufferSize);
+// on openbsd, kMaxBufferSize=60 isn't enough and make the process crash
+// XXX maybe look for actual limit just to know
+//    DCHECK_LE(frame_token_infos_.size(), kMaxBufferSize);
   }
 
   // Registrations should use monotonically increasing frame tokens.
