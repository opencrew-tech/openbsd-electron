Index: scripts/copy-language-packs.mjs
--- scripts/copy-language-packs.mjs.orig
+++ scripts/copy-language-packs.mjs
@@ -41,8 +41,7 @@ export async function afterPack({ appOutDir, packager,
       'Resources'
     );
   } else if (
-    electronPlatformName === 'linux' ||
-    electronPlatformName === 'win32'
+    ['linux', 'openbsd', 'freebsd', 'win32'].includes(electronPlatformName)
   ) {
     // Shared between windows and linux
     // en-US.pak
