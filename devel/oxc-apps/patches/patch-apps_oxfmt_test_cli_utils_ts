Ignore patch(1) backup files created by the ports infrastructure.

Index: apps/oxfmt/test/cli/utils.ts
--- apps/oxfmt/test/cli/utils.ts.orig
+++ apps/oxfmt/test/cli/utils.ts
@@ -140,6 +140,10 @@ export async function runFixture(fixture: Fixture, tes
 
 // --- File reading ---
 
+function shouldIgnoreFixtureFile(path: string): boolean {
+  return path.endsWith(".orig.port");
+}
+
 async function readAllFiles(dir: string): Promise<Map<string, string>> {
   const files = new Map<string, string>();
   const entries = await fs.readdir(dir, { withFileTypes: true, recursive: true });
@@ -148,6 +152,7 @@ async function readAllFiles(dir: string): Promise<Map<
     if (!entry.isFile()) continue;
     const fullPath = join(entry.parentPath, entry.name);
     const relPath = relative(dir, fullPath).replace(/\\/g, "/");
+    if (shouldIgnoreFixtureFile(relPath)) continue;
     files.set(relPath, await fs.readFile(fullPath, "utf8"));
   }
 
@@ -196,6 +201,7 @@ async function detectAndRestoreChanges(
     if (!entry.isFile()) continue;
     const fullPath = join(entry.parentPath, entry.name);
     const relPath = relative(dir, fullPath).replace(/\\/g, "/");
+    if (shouldIgnoreFixtureFile(relPath)) continue;
     if (!filesBefore.has(relPath)) {
       const content = await fs.readFile(fullPath, "utf8");
       created.push({ path: relPath, content });
