Index: app/menu.std.ts
--- app/menu.std.ts.orig
+++ app/menu.std.ts
@@ -152,7 +152,7 @@ export const createTemplate = (
               },
             ]
           : []),
-        ...(devTools && platform !== 'linux' && !process.mas
+        ...(devTools && !(['linux', 'openbsd', 'freebsd'].includes(platform)) && !process.mas
           ? [
               {
                 label: i18n('icu:forceUpdate'),
