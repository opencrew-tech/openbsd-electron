Disable afterPack because fuse is not supported on OpenBSD

Index: package.json
--- package.json.orig
+++ package.json
@@ -140,6 +140,5 @@
     "mac": {
       "category": "public.app-category.productivity"
-    },
+    }
-    "afterPack": "scripts/afterpack.js"
   }
 }
