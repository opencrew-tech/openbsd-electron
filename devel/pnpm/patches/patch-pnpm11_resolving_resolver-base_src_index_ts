Implement pre-fetched archive loading into store.

Index: pnpm11/resolving/resolver-base/src/index.ts
--- pnpm11/resolving/resolver-base/src/index.ts.orig
+++ pnpm11/resolving/resolver-base/src/index.ts
@@ -425,10 +425,16 @@ export type WantedDependency = {
   prevSpecifier?: string
 } & ({
   alias?: string
+  mirror?: string
   bareSpecifier: string
 } | {
   alias: string
+  mirror?: string
   bareSpecifier?: string
+} | {
+  alias?: string
+  mirror: string
+  bareSpecifier: string
 })
 
 export type ResolveFunction = (wantedDependency: WantedDependency & { optional?: boolean }, opts: ResolveOptions) => Promise<ResolveResult>
