Index: sticker-creator/vite.config.ts
--- sticker-creator/vite.config.ts.orig
+++ sticker-creator/vite.config.ts
@@ -14,5 +14,9 @@ export default defineConfig({
   worker: {
     format: 'es',
   },
+  build: {
+    // Avoid Vite 4.5.3 / esbuild 0.28 downleveling issues on the bundled output.
+    target: 'esnext',
+  },
   plugins: [react()],
 });
