fix 'fails if loadurl is called after the navigation is ready to commit' by making it async.
wait for the hook before destroying window (after each test).
XXX need further digging to check why it deviates from linux/others.

Index: electron/spec/api-web-contents-spec.ts
--- electron/spec/api-web-contents-spec.ts.orig
+++ electron/spec/api-web-contents-spec.ts
@@ -507,7 +507,8 @@ describe('webContents module', () => {
     // https://github.com/electron/electron/issues/20008 is resolved
     ifit(!(process.platform === 'win32' && process.arch === 'arm64'))(
       'rejects when loading fails due to DNS not resolved',
-      async () => {
+      async function (this: Mocha.Context) {
+        this.timeout(300000);
         await expect(w.loadURL('https://err.name.not.resolved'))
           .to.eventually.be.rejected()
           .and.have.property('code', 'ERR_NAME_NOT_RESOLVED');
@@ -623,7 +624,8 @@ describe('webContents module', () => {
       await p;
     });
 
-    it("doesn't reject when a subframe fails to load", async () => {
+    it("doesn't reject when a subframe fails to load", async function (this: Mocha.Context) {
+      this.timeout(300000);
       let resp = null as unknown as http.ServerResponse;
       s = http.createServer((req, res) => {
         res.writeHead(200, { 'Content-Type': 'text/html' });
@@ -1277,7 +1279,7 @@ describe('webContents module', () => {
     // back to OpenFolder() which does a blocking DirectoryExists() on the UI
     // thread (pre-existing behavior). Workspace-gating is covered by the test
     // above.
-    ifit(process.platform !== 'linux')(
+    ifit(!['freebsd', 'linux', 'openbsd'].includes(process.platform))(
       'reveals paths under a registered workspace folder without executing them',
       async () => {
         const w = new BrowserWindow({ show: false });
@@ -2993,7 +2995,8 @@ describe('webContents module', () => {
 
     type PageSizeString = Exclude<Required<Electron.PrintToPDFOptions>['pageSize'], Electron.Size>;
 
-    it('with custom page sizes', async () => {
+    it('with custom page sizes', async function (this: Mocha.Context) {
+      this.timeout(300000);
       const paperFormats: Record<PageSizeString, ElectronInternal.PageSize> = {
         Letter: { width: 8.5, height: 11 },
         Legal: { width: 8.5, height: 14 },
@@ -3095,7 +3098,7 @@ describe('webContents module', () => {
     });
 
     // TODO(codebytere): OOPIF printing is disabled on Linux at the moment due to crashes.
-    ifit(process.platform !== 'linux')('can print cross-origin iframes', async () => {
+    ifit(!(['freebsd', 'linux', 'openbsd'].includes(process.platform)))('can print cross-origin iframes', async () => {
       server = http.createServer((_, res) => {
         res.writeHead(200);
         res.end(`
