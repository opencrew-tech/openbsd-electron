Index: ts/services/notifications.preload.ts
--- ts/services/notifications.preload.ts.orig
+++ ts/services/notifications.preload.ts
@@ -195,7 +195,7 @@ class NotificationService extends EventEmitter {
       this.#lastNotification?.close();
 
       const notification = new window.Notification(title, {
-        body: OS.isLinux() ? filterNotificationText(message) : message,
+        body: (OS.isLinux() || OS.isBSD()) ? filterNotificationText(message) : message,
         icon: iconUrl,
         silent: true,
         tag: messageId,
