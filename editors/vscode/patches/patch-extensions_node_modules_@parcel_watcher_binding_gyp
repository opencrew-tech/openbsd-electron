kqueue backend leaks fd

watchman needs F_GETPATH kernel support and a dedicated port

inotify backend works on a single file at start but later crashes
[File Watcher (universal)] ... error: terminated by itself
unexpectedly with code 9, signal: unknown (ETERM)

keep linking with inotify until kqueue backend gets fixed

Index: extensions/node_modules/@parcel/watcher/binding.gyp
--- extensions/node_modules/@parcel/watcher/binding.gyp.orig
+++ extensions/node_modules/@parcel/watcher/binding.gyp
@@ -35,7 +35,7 @@
             "ARCHS": ["arm64"]
           }
         }],
-        ['OS=="linux" or OS=="android"', {
+        ['OS=="linux" or OS=="android" or OS=="openbsd"', {
           "sources": [
             "src/watchman/BSER.cc",
             "src/watchman/WatchmanBackend.cc",
@@ -80,6 +80,11 @@
               ]
             }
           }
+        }],
+        ['OS=="openbsd"', {
+          "libraries": [
+            "-lnotify"
+          ]
         }],
         ['OS=="freebsd"', {
           "sources": [
