Index: download_from_google_storage.py
--- download_from_google_storage.py.orig
+++ download_from_google_storage.py
@@ -38,6 +38,9 @@ PLATFORM_MAPPING = {
     "zos": "zos",
 }
 
+if sys.platform.startswith("openbsd"):
+    PLATFORM_MAPPING[sys.platform] = "linux"
+
 # (b/328065301): Remove when all GCS hooks are migrated to first class deps
 MIGRATION_TOGGLE_FILE_SUFFIX = "_is_first_class_gcs"
 
@@ -100,7 +103,7 @@ class Gsutil(object):
     RETRY_BASE_DELAY = 5.0
     RETRY_DELAY_MULTIPLE = 1.3
     VPYTHON3 = (
-        "vpython3.bat" if GetNormalizedPlatform() == "win32" else "vpython3"
+        "vpython3.bat" if GetNormalizedPlatform() == "win32" else sys.executable
     )
 
     def __init__(self, path, boto_path=None):
