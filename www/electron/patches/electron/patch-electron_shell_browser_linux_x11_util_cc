Index: electron/shell/browser/linux/x11_util.cc
--- electron/shell/browser/linux/x11_util.cc.orig
+++ electron/shell/browser/linux/x11_util.cc
@@ -7,10 +7,12 @@
 #include "build/build_config.h"
 #include "ui/ozone/platform_selection.h"  // nogncheck
 
+#include "ui/base/ozone_buildflags.h"
+
 namespace x11_util {
 
 bool IsX11() {
-#if BUILDFLAG(IS_LINUX)
+#if BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_BSD)
   static const bool is = ui::GetOzonePlatformId() == ui::kPlatformX11;
   return is;
 #else
@@ -19,7 +21,7 @@ bool IsX11() {
 }
 
 bool IsWayland() {
-#if BUILDFLAG(IS_LINUX)
+#if BUILDFLAG(SUPPORTS_OZONE_WAYLAND) && (BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_BSD))
   static const bool is = ui::GetOzonePlatformId() == ui::kPlatformWayland;
   return is;
 #else
