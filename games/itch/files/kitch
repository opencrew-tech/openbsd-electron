#!/bin/sh
ELECTRON_IS_DEV=0 \
ELECTRON_FORCE_IS_PACKAGED=1 \
NODE_ENV=production \
ELECTRON_APP_NAME="${MODELECTRON_APP_NAME}" \
ELECTRON_UNVEIL="${MODELECTRON_UNVEIL_FILE}" \
BROTH_USE_LOCAL=butler \
exec ${TRUEPREFIX}/${ELECTRON_WRAPPER} \
	--app="${TRUEPREFIX}/${MODELECTRON_DIST_T}/" $@
