Index: gclient.py
--- gclient.py.orig
+++ gclient.py
@@ -3333,6 +3333,8 @@ class CipdDependency(Dependency):
         condition,
     ):
         package = dep_value["package"]
+        if sys.platform.startswith("openbsd"):
+            package = package.replace("${platform}", "linux-${arch}")
         if dep_value.get("version_file"):
             fp = os.path.join(
                 parent.root.root_dir, parent.name, dep_value["version_file"]
