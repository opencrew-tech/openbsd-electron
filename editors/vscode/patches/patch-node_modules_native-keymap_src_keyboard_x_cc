OpenBSD is not supported by native-keymap. Return an empty mapping without
trying to use its X11 implementation.

Index: node_modules/native-keymap/src/keyboard_x.cc
--- node_modules/native-keymap/src/keyboard_x.cc.orig
+++ node_modules/native-keymap/src/keyboard_x.cc
@@ -167,6 +167,10 @@ napi_value GetKeyMapImpl(napi_env env, napi_callback_i
   napi_value result;
   NAPI_CALL(env, napi_create_object(env, &result));
 
+#if defined(__OpenBSD__)
+  return result;
+#endif
+
   Display *display;
   if (!(display = XOpenDisplay(""))) {
     return result;
