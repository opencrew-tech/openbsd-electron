Index: ts/test-electron/models/messages_test.preload.ts
--- ts/test-electron/models/messages_test.preload.ts.orig
+++ ts/test-electron/models/messages_test.preload.ts
@@ -670,6 +670,9 @@ describe('Message', () => {
         isLinux() {
           return false;
         },
+        isBSD() {
+          return false;
+        },
       });
       const message = createMessage({
         conversationId: (
@@ -697,6 +700,9 @@ describe('Message', () => {
       this.sandbox.replace(window.Signal, 'OS', {
         ...window.Signal.OS,
         isLinux() {
+          return true;
+        },
+        isBSD() {
           return true;
         },
       });
