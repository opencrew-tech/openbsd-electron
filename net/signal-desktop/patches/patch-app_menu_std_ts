Index: app/menu.std.ts
--- app/menu.std.ts.orig
+++ app/menu.std.ts
@@ -151,7 +151,7 @@ export const createTemplate = (
               },
             ]
           : []),
-        ...(devTools && platform !== 'linux'
+        ...(devTools && !(['linux', 'openbsd', 'freebsd'].includes(platform))
           ? [
               {
                 label: i18n('icu:forceUpdate'),
