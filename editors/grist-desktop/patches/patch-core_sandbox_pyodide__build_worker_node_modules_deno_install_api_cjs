Index: core/sandbox/pyodide/_build/worker/node_modules/deno/install_api.cjs
--- core/sandbox/pyodide/_build/worker/node_modules/deno/install_api.cjs.orig
+++ core/sandbox/pyodide/_build/worker/node_modules/deno/install_api.cjs
@@ -11,6 +11,11 @@ let cachedIsMusl = undefined;
 module.exports = {
   runInstall() {
     const denoFileName = os.platform() === "win32" ? "deno.exe" : "deno";
+    const envDenoPath = process.env.DENO_INSTALL_BIN;
+    if (envDenoPath && fs.existsSync(envDenoPath)) {
+      return envDenoPath;
+    }
+
     const targetExecutablePath = path.join(
       __dirname,
       denoFileName,
