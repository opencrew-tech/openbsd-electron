     SUBDIR =
     SUBDIR += app-builder
     SUBDIR += ast-grep
     SUBDIR += berry
     SUBDIR += esbuild
     SUBDIR += gclient
     SUBDIR += go-bindata
     SUBDIR += node-graceful-fs
     SUBDIR += npm
     SUBDIR += parcel-watcher
     SUBDIR += pnpm
     SUBDIR += py-dbusmock
     SUBDIR += rollup
     SUBDIR += yarn

.include <bsd.port.subdir.mk>
