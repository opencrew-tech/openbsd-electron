Use tsc instead of tsgo, avoid a dedicated port for now.

Add extra --skipLibCheck arg.
Avoid build error on old @types/markdown-it version.
Avoid updating depends for now.

Index: build/lib/tsgo.ts
--- build/lib/tsgo.ts.orig
+++ build/lib/tsgo.ts
@@ -26,13 +26,13 @@ export function spawnTsgo(projectPath: string, config:
 		}
 	}
 
-	const args = ['tsgo', '--project', projectPath, '--pretty', 'false'];
+	const args = ['--skipLibCheck', '--project', projectPath, '--pretty', 'false'];
 	if (config.noEmit) {
 		args.push('--noEmit');
 	} else {
 		args.push('--sourceMap', '--inlineSources');
 	}
-	const child = cp.spawn(npx, args, {
+	const child = cp.spawn('node_modules/.bin/tsc', args, {
 		cwd: root,
 		stdio: ['ignore', 'pipe', 'pipe'],
 		shell: true
